# Backend Service
# Railway will auto-detect this as Node.js

[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["node"]

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/api/health"
